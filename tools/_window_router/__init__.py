"""Private implementation modules for ``tools/train_window_router.py``.

This package is deliberately private to the CLI — its stability is
not guaranteed for direct import. The public surface is the
``train_window_router`` argparse entry point.
"""
